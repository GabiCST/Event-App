<Window
    x:Class="Event_App.Account_Verification"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Event_App"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Account Verification"
    Width="400"
    Height="350"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <StackPanel>
        <TextBlock HorizontalAlignment="Center" Text="Enter your username and email addres for verification" />
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label
                Grid.Row="0"
                Grid.Column="0"
                Margin="20"
                VerticalAlignment="Center"
                Content="Username:" />
            <TextBox
                x:Name="Username"
                Grid.Row="0"
                Grid.Column="1"
                Width="200"
                Height="25" />
            <Label
                Grid.Row="1"
                Grid.Column="0"
                Margin="20"
                VerticalAlignment="Center"
                Content="Email:" />
            <TextBox
                x:Name="Email"
                Grid.Row="1"
                Grid.Column="1"
                Width="200"
                Height="25" />
        </Grid>
        <Button
            Width="200"
            Margin="0,20,0,0"
            HorizontalAlignment="Center"
            Click="Verify_Button"
            Content="Verify Account" />
        <Button
            Width="100"
            Margin="0,20,0,0"
            HorizontalAlignment="Center"
            Click="Back_Button"
            Content="Back" />

    </StackPanel>
</Window>
